<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Claim Coupon</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container d-flex justify-content-center align-items-center" style="min-height: 100vh;">
    <div class="card shadow p-4" style="max-width: 500px; width: 100%;">
      <h2 class="text-center text-success mb-4">🎉 Claim Your Coupon</h2>

      <% if (typeof message !== 'undefined') { %>
        <div class="alert <%= success ? 'alert-success' : 'alert-danger' %> text-center">
          <%= message %>
        </div>

      <% } %>

      <% if (typeof couponCode !== 'undefined') { %>
        <div class="text-center mt-4">
          <h4 class="text-primary">Hi, <%= username %>! 🎁</h4>
          <p class="mt-3 fw-semibold">Your coupon code is:</p>
          <div class="alert alert-success fs-4 fw-bold">
            <%= couponCode %>
          </div>
          <p class="text-muted">Use this code at checkout to get your reward!</p>
        </div>
      <% } else { %>
        <form method="POST" action="/claim">
          <div class="mb-3">
            <label for="username" class="form-label">Your Name</label>
            <input type="text" class="form-control" id="username" name="username" required placeholder="Enter your name">
          </div>
          <div class="d-grid">
            <button type="submit" class="btn btn-primary">Claim Coupon</button>
          </div>
        </form>
      <% } %>

    </div>
  </div>

</body>
</html>
